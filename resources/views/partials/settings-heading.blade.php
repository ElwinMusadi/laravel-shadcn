<div class="relative mb-6 w-full">
    <x-ui.heading variant="page">{{ __('Settings') }}</x-ui.heading>
    <p class="mb-6 mt-2 text-base text-muted-foreground">{{ __('Manage your profile and account settings') }}</p>
    <x-ui.separator />
</div>
